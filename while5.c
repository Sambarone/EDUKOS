#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int br,suma,i;

	suma=0; 
	printf("Unesi neki pozitivan broj: ");
	scanf("%d",&br);
	for(i=1;i<=n;i++){
	suma=suma+i;
	}
		}
	printf("Suma je %d !",suma);
	return 0;
}
